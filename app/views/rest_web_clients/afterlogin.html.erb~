<% if flash[:alert] %>
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title" id="todo-label">Meldung</h4>
      </div>
      <div class="modal-body"> 
	<% flash.each do |name, msg| -%>
      		<%= content_tag :div, msg, class: name %>
		<br>
    	<% end -%>

      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-dismiss="modal">OK</button>
      </div>
    </div>
   </div>
<% end %>
<table class="table table-striped">
  <tr>
    <td>Nachricht verschicken</td>
    <td>Nachricht abrufen</td>
  </tr>
  <tr>
    <td><%= form_for @rest_web_client, url: {action: "sendMessageAction"} do |f| %>
          <% if @rest_web_client.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@rest_web_client.errors.count, "error") %> prohibited this rest_web_client from being saved:</h2>

                <ul>
                  <% @rest_web_client.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
          <% end %>
          <div class="field">
            <%= f.label :username %><br>
            <%= f.text_field :username %>
          </div>
          <div class="field">
            <%= f.label :password %><br>
            <%= f.text_field :password %>
          </div>
          <div class="field">
            <%= f.label :msg %><br>
            <%= f.text_field :msg %>
          </div>

          <div class="field">
            <%= f.label :receiver %><br>
            <%= f.text_field :receiver %>
          </div>
	<br>
    <%= f.submit :class => "btn btn-success btn-lg" %>
      <% end %>
    </td>
    <td><%= form_for @rest_web_client, url: {action: "receiveAction"} do |f| %>
          <% if @rest_web_client.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@rest_web_client.errors.count, "error") %> prohibited this rest_web_client from being saved:</h2>

                <ul>
                  <% @rest_web_client.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
          <% end %>

          <div class="field">
            <%= f.label :username %><br>
            <%= f.text_field :username %>
          </div>
          </div>
	<br>
    <%= f.submit :class => "btn btn-success btn-lg" %>
      <% end %>
      </td>
  </tr>
</table>
<div class="modal fade" id="alert" tabindex="-1" role="dialog" aria-labelledby="todo-label" aria-hidden="true">
  
</div>
